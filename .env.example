DB_USERNAME=caritakip
DB_PASSWORD=changeme
DB_NAME=caritakip

JWT_SECRET=change-this-jwt-secret-very-long-random-string
JWT_REFRESH_SECRET=change-this-refresh-secret-very-long-random-string

MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=invoices

# Tablo şemasını entity'lere göre otomatik günceller (migration yokken gerekli).
DB_SYNCHRONIZE=true
# SQL sorgularını loglar (sadece hata ayıklarken true yap).
DB_LOGGING=false

# Web panelinin adresi (API sadece bu origin'den gelen tarayıcı isteklerini kabul eder)
CORS_ORIGIN=https://cari.marifoglu.trade

# Yeni kullanıcı kaydı (varsayılan kapalı). Arkadaş eklerken geçici olarak true yap.
REGISTER_ENABLED=false
